Formatting
  + Check Exception
  + functions: lower_case_underscore
  + constants: UPPER_CASE_ALL
  + Avoid bool == True condition

halosnap: can turn it into a better app
windsnap: visualization tool that can be good

plotlib: Import the plotting libraries in.
  + Frame
  + Smart legend and colorbar
  + Add particles to PhaseDiagram
  + Single panel line plots:
    - Set resolution range
    - Set xlim, ylim
    - Add model
    - Add data
    + Overrides:
      + Galactic stellar mass function
      + Stellar mass - halo mass function
      + MZR, fgas
  + Snapshot 2D Map
  + halo 3D Map

snapshot.py:
  + Add function callers
    - e.g., from _analysis import *

analysis.py:
  + Add halo radial profile calculator
  
configuration files
  + Separate simulation.cfg from pygizmo.cfg (or not)